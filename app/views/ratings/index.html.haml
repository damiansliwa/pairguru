%section
  %table.table.table-striped
    %thead
      %tr
        %td No
        %td User name
        %td Comments count
    %tbody
      - @users_with_most_comments.each_with_index do |user, index|
        %tr
          %td= index + 1
          %td= user.email
          %td= user.comments.size
